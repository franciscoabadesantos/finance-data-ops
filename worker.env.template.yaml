SUPABASE_URL: "https://your-project-id.supabase.co"
SUPABASE_SERVICE_ROLE_KEY: "__FROM_SECRET_MANAGER__"
FINANCE_DATA_OPS_ROOT: "/app"

GCP_PROJECT_ID: "your-gcp-project-id"
GCP_LOCATION: "us-central1"
CLOUD_TASKS_QUEUE: "ticker-jobs"
CLOUD_TASKS_ENABLED: "true"
WORKER_BASE_URL: "https://<cloud-run-worker-url>"
TASKS_INVOKER_SERVICE_ACCOUNT_EMAIL: "finance-tasks-invoker@your-gcp-project-id.iam.gserviceaccount.com"

# Optional defense-in-depth bearer secret.
# Prefer Cloud Run IAM/OIDC auth and leave this unset unless required.
WORKER_SHARED_TOKEN: "__OPTIONAL_FROM_SECRET_MANAGER__"
